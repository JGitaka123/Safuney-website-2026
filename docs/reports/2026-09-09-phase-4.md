# Phase 4 — B2B: accounts, approvals, price lists, credit, quotes

**Date:** 2026-09-09 · **Branch:** `claude/funny-thompson-2lukly` → PR to `main` · Plan: `docs/plans/phase-4-b2b.md`

## What shipped
- **Sign-in** (Auth.js v5, ADR 0008): six-digit code by SMS or email, or an emailed link; no customer
  passwords. Staff: email + password + authenticator code. Guest cart merges into the account on
  sign-in; the header shows Account or Sign in without making pages dynamic.
- **Organisation accounts**: owner creates the organisation (name, registered name, KRA PIN, approval
  threshold), adds buyers, approvers and owners by email; members see the organisation at checkout
  and orders are attached to it with the purchase-order number.
- **Approval workflow**: a buyer's order at or above the threshold is placed as *awaiting approval*
  with stock held for seven days; approvers and owners decide from `/account/approvals`; approval
  confirms (cash, invoice) or starts payment (M-Pesa, card); rejection cancels and releases stock;
  every step is an event with the actor. Unapproved orders are cancelled by the cron after the hold.
- **Price lists**: tiered customer prices (`PriceListItem.minQty`) resolved on the server at order
  placement; the client never sends prices. Admin tooling for price lists is Phase 6.
- **Credit accounts**: application from the account (registered name, KRA PIN, limit, terms, trade
  references, document links); finance decides at `/sales/credit`. Invoice payment is offered only to
  approved organisations and only within the available credit (limit minus unpaid tax invoices),
  checked inside the order transaction with the customer row locked. Tax invoices
  (`INV-YYYYMMDD-NNNN`, KRA fields, itemised VAT, due date from terms) are issued on confirmation and
  downloadable as PDF; `/account/credit` is the statement of account (limit, outstanding, available,
  overdue, invoices).
- **Saved lists and reorder**: any order becomes a list in one tap; lists add to the cart in one tap
  with per-line stock reporting; one-click reorder from order history.
- **Scheduled deliveries**: a cart becomes a standing order (weekly to eight-weekly); reminder email
  three days ahead; daily cron places the due orders; skip, pause, resume, cancel from the account.
- **Request for quote**: `/quote` for guests and customers (free text and/or the cart); sales prices
  each line against a catalogue pack at `/sales/quotes`; the customer accepts from an emailed link and
  the quote becomes an order at the quoted prices (audited on the order event). Quotes expire.
- Sales desk at `/sales` (quotes to price, credit applications) for SALES, FINANCE and ADMIN roles.

## Verified (local production build, mock payments and mock sign-in)
- Gate tests: approval workflow (buyer cannot approve, approver can, rejection releases stock, credit
  is re-checked at approval), credit-limit enforcement (refused above the available credit with the
  amount named, freed when an invoice is paid, invoice issued with a 30-day due date), price-list
  tiers, organisation authorisation (owner-only invites, last owner cannot be demoted), finance-only
  credit decisions, quote lifecycle (request → price → accept → order at quoted price; expiry;
  decline). 102 unit and integration tests in `apps/web`, 33 in the packages.
- Playwright + axe, mobile first and desktop: sign-in (validation, wrong code, staff refusal), sign-in
  keeps the guest cart, order appears in the account, sign-out; owner creates an organisation, invites
  a buyer, the buyer's large order waits, the owner approves; saved list from an order and add-all;
  scheduled delivery from the cart with skip and pause; quote request → sales pricing → acceptance →
  order at the quoted price. 106 e2e tests green.
- Lighthouse: see the table in the PR checks (`/sign-in` and `/quote` added to the page list).

## Not done / blocked
- **Real SMS and email delivery are unverified**: no Africa's Talking or Resend keys on previews yet.
  The flows run in mock mode (code shown on the page).
- File uploads for credit applications: links for now; uploads arrive with the admin console.
- Delivery addresses on the account (scheduled deliveries collect from Mombasa Road until Phase 5 adds
  address books).
- Staff enrol their authenticator in Phase 6 admin; until then demo staff sign in with password only.

## PO actions needed
1. Add `AUTH_SECRET` to Vercel (Production and Preview, different values):
   `openssl rand -base64 32`.
2. Add `AT_API_KEY`, `AT_USERNAME`, `AT_SENDER_ID` (Africa's Talking) to Preview so a real SMS code
   can be tested; `RESEND_API_KEY` for emailed links and invoices.
3. Confirm the credit-terms policy (question 9): default terms offered (14/30/45/60 days), who
   approves, and the KRA PIN / VAT number for tax invoices (`COMPANY_KRA_PIN`, `COMPANY_VAT_NUMBER`).
4. Try the organisation flow on the preview: create an organisation, invite a colleague, place an
   order above the threshold, approve it.

## Risks I see that you didn't ask about
- Auth.js v5 is still tagged beta; the API used is its stable subset and the upgrade is a version
  bump, but it is the one dependency in the stack without a stable release.
- Invoice credit is enforced by the site; payment recording (marking invoices paid) is a finance action
  that lands with Phase 6. Until then a paid invoice is marked paid in the database by hand.
- A buyer whose email is invited but who signs in by phone gets a separate account. The invitation is
  by email, so the sign-in page tells them to use the invited email; Phase 5 adds linking a phone to an
  existing account.
